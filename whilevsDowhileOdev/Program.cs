using System.Reflection.Metadata;

int sayac = 0;
Console.WriteLine("Lütfen bir limit değer giriniz :");
int Limit = int.Parse(Console.ReadLine());
while (sayac <= Limit)
{
  
    Console.WriteLine("Ben bir Patika' lıyım");
    sayac++;
}
